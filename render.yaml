# Render web service, built from the Dockerfile. Secrets are not stored here:
# in the Render dashboard use Environment > "Add from .env" and paste render.env.
services:
  - type: web
    name: sdoc
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
